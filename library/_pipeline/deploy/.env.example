# Live path: /opt/igaming-library/_pipeline/deploy/.env
# Copy, fill, never commit. See library/_pipeline/SECRETS.md

CAPTURE_API_TOKEN=replace-with-long-random
LIBRARY_HOST_PATH=/opt/igaming-library
LIBRARY_ROOT=/data/library
PIPELINE_ROOT=/data/library/_pipeline
PORT=8787
WORKER_CONCURRENCY=2
WORKER_MAX_PARALLEL=2
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
DISPLAY=:99

# Optional — overrides secrets/gemini.key
# GEMINI_API_KEY=
# YTDLP_COOKIES=/data/library/_pipeline/secrets/youtube.cookies.txt
